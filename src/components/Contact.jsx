import React from 'react';
import styled from 'styled-components';

const StyledContainer = styled.div``;

const Contact = (id, first_name, last_name, email, gender, avarar) => {
  return (
    <StyledContainer>
      <h1>Contact</h1>
    </StyledContainer>
  );
};

export default Contact;
